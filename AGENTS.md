# Agents

Bun monorepo with packages: `client` (Vite+React), `server` (PartyKit), `shared`.

## Typechecking

Run `bun run typecheck` at the repo root to typecheck all packages.
For a single package: `bun --filter @fake-goes-party/<name> typecheck`.
